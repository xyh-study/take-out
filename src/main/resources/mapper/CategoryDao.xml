<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njpi.xyh.takeout.dao.CategoryDao">

    <resultMap type="com.njpi.xyh.takeout.entity.Category" id="CategoryMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="type" column="type" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="sort" column="sort" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="createUser" column="create_user" jdbcType="INTEGER"/>
        <result property="updateUser" column="update_user" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into reggie.category(type, name, sort, create_time, update_time, create_user, update_user)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.type}, #{entity.name}, #{entity.sort}, #{entity.createTime}, #{entity.updateTime},
            #{entity.createUser}, #{entity.updateUser})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into reggie.category(type, name, sort, create_time, update_time, create_user, update_user)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.type}, #{entity.name}, #{entity.sort}, #{entity.createTime}, #{entity.updateTime},
            #{entity.createUser}, #{entity.updateUser})
        </foreach>
        on duplicate key update
        type = values(type) , name = values(name) , sort = values(sort) , create_time = values(create_time) ,
        update_time = values(update_time) , create_user = values(create_user) , update_user = values(update_user)
    </insert>

</mapper>

